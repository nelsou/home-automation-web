CREATE TABLE syf_user
(
    name VARCHAR(255) PRIMARY KEY NOT NULL,
    draw INT NOT NULL,
    lost INT NOT NULL,
    win INT NOT NULL,
    current_win_inaraw INT NOT NULL,
    max_win_inaraw INT NOT NULL
);
